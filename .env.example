# Miss Floss / Dogra — env scaffold
# This static landing does not consume any env vars at build time.
# Values below are for the FastAPI + Pipecat voice stack (rapidx-voice fork) when deployed on a VPS.

# Supabase (DograMissfloss project — ap-south-1)
SUPABASE_URL=https://uazwppofnntmdwqoqyuf.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Auth (Dograh OSS JWT)
OSS_JWT_SECRET=

# Gemini Live (voice model)
GEMINI_API_KEY=
GEMINI_REALTIME_MODEL=gemini-3.1-flash-live-preview

# Vobiz telephony (SIP / DID)
VOBIZ_AUTH_ID=
VOBIZ_AUTH_TOKEN=
VOBIZ_APPLICATION_ID=

# Public app URL (used by Vobiz answer_url)
BACKEND_API_ENDPOINT=
